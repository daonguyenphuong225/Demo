git init                    // khởi tạo giá trị dưới máy
git add .                   // thêm tất cả các file để GIT quản lý

........ báo danh
git config --global user.name "tên"
git config --global user.email "email cac em su dung"

git commit -m "Nội dung comment"                         //tạo 1 bản ghi chú 
git remote add origin "link trang git"                  //liên kết dưới máy với github trên mạng

.......... bắt đầu nhập mk GIT
git push -u origin master                               //Đẩy comit lên github
nhập username
nhập password
git config --global credential.helper store            //Lưu tài khoản GIT




=====================
git add
git comit -m "NỘi dung comment"
git push